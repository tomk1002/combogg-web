---
description: 타입체크·린트·테스트를 순서대로 실행하고 결과 보고
---

# /check

다음을 순서대로 실행하고 결과를 요약해주세요.

1. `pnpm typecheck`
2. `pnpm lint`
3. `pnpm test`

각 단계에서:
- 성공 → 다음 단계로
- 실패 → 원인 분석 후 수정안 제시 (자동 수정하지 말 것, 먼저 보고)

마지막에 한 줄 요약: `✅ 모두 통과` 또는 `❌ N개 실패 — 수정 필요`
